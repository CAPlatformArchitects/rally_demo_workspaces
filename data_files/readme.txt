These files are the format for importing data into the database.
The database is rally_data.
Each CSV file represents a table in which information is imported.
The easiest way to import data is to use the \copy command in psql.
